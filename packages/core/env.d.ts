/**
 * Author-facing ambient types for Aero templates.
 *
 * Include via `"types": ["@aero-js/core/env"]` or `/// <reference types="@aero-js/core/env" />`.
 *
 * Canonical declarations live in `@aero-js/compiler/env`; this file re-exports them so apps
 * that already depend on `@aero-js/core` do not need a direct `@aero-js/compiler` dependency.
 */
/// <reference types="@aero-js/compiler/env" />
